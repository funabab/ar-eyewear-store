---
import { Icon } from 'astro-icon'
interface Props extends astroHTML.JSX.HTMLAttributes {}
const { class: className, ...props } = Astro.props
---

<header class:list={[className, 'py-10']} {...props}>
  <Icon name="eva:grid-fill" class="h-5" />
</header>
